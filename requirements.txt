# Dzhira backend dependencies.
fastapi>=0.110
uvicorn[standard]>=0.29        # includes the websockets impl the /ws endpoint needs
watchdog>=4.0

# Dev / tests
pytest>=8.0
httpx>=0.27                    # FastAPI TestClient transport
